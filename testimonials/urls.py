from django.conf.urls.defaults import *
from testimonials.models import Testimonial

urlpatterns = patterns('',
    #url(r'^$', 'testimonials.views.index', name='testimonials_list'),
    #url(r'^add/$', 'testimonials.views.add', name='testimonial_add'),
    #rate
)
